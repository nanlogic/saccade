# Developer Preview 0.3.22 convergence report

Date: 2026-08-14

## Candidate

Extension version: `0.3.22`

Content identity:
`225aa5adbe6c7a237b2cc110543625e98d3d640498cd39bcfe3c7fcad30592c4`

The source candidate, installed Extension, live Chrome and Edge handshake,
Runtime expectation, and setup release metadata used the same identity.

## Final local evidence

The final denominator is stored at
`~/Library/Application Support/Saccade Dev/evidence/20260814T184413Z/denominator-63.json`.
It reports 63 total rows: 56 local passes, 7 truthful limitations, zero local
blockers, and 63 publication blockers. Chrome and Edge both passed lifecycle,
pushed-delta, Resource subscription, stream gap/reset, and 137/137 latency
events. Chrome p95 was 28.155 ms; Edge p95 was 27.932 ms.

The strict no-window evidence is stored at
`~/Library/Application Support/Saccade Dev/evidence/20260814-final-no-window-0.3.22.json`.
Both cycles began with no normal Chrome-for-Testing window, recovered through
the validated Extension wake surface, observed all 63 fixture objects, and
closed the Agent-owned tab. Recovery took 1.90 s and 1.72 s.

Static gates passed: 57 Rust tests, 30 Extension tests, 57 Python tests, 13
setup tests, architecture gate, generated-file idempotence, formatting, and
diff whitespace checks. Clippy passed after its one mechanical borrow fix.

## Setup draft

`@saccade/setup` remains version `0.1.0` and `published: false`.
The six-file npm dry run is 8.4 kB. The unpublished macOS arm64 Runtime draft
is under ignored `dist/setup-preview-0.3.22`; its SHA-256 is
`1628b5cb0107b967885a4fe21c85478fc80a3e5c9912c6f4e8359e86717e1504`.
It is unsigned, has no download URL, and has no store origins.

## Honest remaining gates

- Complete six valid order-reversed Saccade/official Playwright MCP runs and
  verify the frozen official version online before any superiority claim.
- Produce source-diverse public Codex/Claude same-tab evidence for every
  Catalog row to be promoted. Local fixtures and Reference Actuator evidence
  cannot promote rows.
- Log Claude Code in and complete one equivalent same-browser loop.
- Obtain npm scope ownership, signing material, and final Chrome/Edge store
  identities; then freeze a commit, versions, browser versions, origins, and
  checksums and rerun the complete release matrix.
