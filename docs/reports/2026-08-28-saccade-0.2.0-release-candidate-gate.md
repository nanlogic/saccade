# Saccade 0.2.0 release-candidate gate

Date: 2026-08-29 America/Chicago
Status: local release candidate passed; commit, remote CI, tag, and publication pending

## Candidate

- Product package: `@nanlogic/saccade@0.2.0`
- Extension version: `0.4.0`
- Extension candidate: `921de39f7cd78f6c6ca744ca50c496577aa7e96f7d3da372c8c9b649235e0445`
- Production route: Chrome/Edge Extension → loopback Node Broker → MCP
- Rust, Native Host, platform driver, Playwright/CDP fallback: absent

## Blocking gates

The unified gate completed successfully with the exact candidate attached once
in Chrome and once in Edge. Both browsers passed:

- same-origin iframe, restricted iframe, and open Shadow DOM Truth;
- semantic table projection;
- fully preflighted form batch and separate contenteditable action;
- standard object-addressed file upload with observed semantic postcondition;
- two-Agent session isolation, cross-read denial, exact tab inventory, and one
  writer per tab;
- 24/24 normal moving-control targets and 24/24 Canvas reflex targets.

The final deterministic run retained millisecond-scale normal-control and
Canvas response in both browsers with 24/24 hits and zero misses in every lane.

## Advisory public compatibility

Public compatibility remains advisory because third-party layout, hydration,
and availability are not controlled by the release. Repeated release runs
passed every listed case in both browsers. One final Chrome Angular run
correctly rejected two case-normalized `Favorite food` candidates as ambiguous
instead of guessing; this advisory page-layout variance did not affect the
deterministic release result:

- Selenium official web form;
- DemoQA React practice form;
- Angular Material select open and verified option selection;
- Best Buy homepage navigation Truth;
- NanMesh, Nanlogic, and Mythcast Era homepage Truth.

No public form Submit control was activated.

## Static and package evidence

- Node/Extension tests: complete suites passed with one sandbox-only loopback
  test skipped.
- Single-production-architecture check: passed.
- `git diff --check`: passed.
- Markdown authority integrity check: passed with only the preserved legacy
  `AGENTS.md` adoption warning.
- Extension ZIP: `saccade-extension-0.4.0.zip`, 70,185 bytes,
  SHA-256 `2ce74a2c0165ccc6ab461bacedb67cf678fd887a28fae9502f581469ec779934`.
- npm pack dry-run: `@nanlogic/saccade@0.2.0`, eight files, no install hook.
- Agent payload proof: initial working-set Truth 3,493 bytes, batch receipt 972
  bytes, and total short-form tool output 8,629 bytes.
- Full local machine report SHA-256:
  `ef6fbdf466063eb077117b3b876babc3bc90e6d0da9edc80a04e68f7a08ce3a8`.

## Remaining release work

1. Review and commit the complete release tree without absorbing unrelated
   local material.
2. Run the committed candidate through the configured Node 18/20/22/24 CI
   matrix.
3. Tag and publish only after explicit owner authorization.

Authenticated Steamworks native-trusted file selection remains a truthful,
non-blocking limitation. It is not replaced with a Native Host, platform
driver, CDP, Playwright, or site-specific upload route.
