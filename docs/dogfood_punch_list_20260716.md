# Saccade Dogfood Punch List: 2026-07-16

Status: fixes and evidence are tracked in the acceptance checklist

This list captures the first installed AI-041 dogfood feedback. Confirmed
defects are separated from regression checks so that later work starts from
measured behavior instead of assumptions.

## Confirmed defects

| ID | Priority | Area | Observed behavior | Expected behavior | Done when |
| --- | --- | --- | --- | --- | --- |
| DF-001 | P0 | Link / child-page lifecycle | Fixed in `ai042-tab-routing-20260716`. On `https://www.nanmesh.ai/agents`, a user-opened page had created a separate native window because the pinned CEF `SimpleHandler` had no new-tab override. | A normal user-gesture `NEW_FOREGROUND_TAB` or `NEW_BACKGROUND_TAB` opens in the existing Saccade window. Explicit popup/dialog/OAuth dispositions remain separate windows. | PASS: source regression `runs/cef_day5/session_tab_routing_20260716/report.json`, packaged regression `runs/cef_day5/session_tab_routing_packaged_final2_20260716/report.json`, and human live NaNMesh quickstart testing all passed. |
| DF-002 | P0 | Keychain UX | Current `ai042` kit passed same-build saved-profile relaunch testing. Earlier the user reported Saccade asking for Keychain access again even though packages use the same Team ID `48KK2UWXQM`; no prompt reproduced during the current check. | One understandable first-use prompt at most for the saved profile; subsequent signed builds with the same designated requirement reuse the approved Keychain item without repeated prompts. Incognito should not depend on saved-profile cookie storage. | PASS for current kit: three relaunches through `bin/open-saccade` produced no Keychain prompt by human observation. Re-check on the next replacement build. Do not use `open -a Saccade.app <url>` as evidence because macOS can route the URL to another browser. |
| DF-003 | P1 | Installer completeness | Fixed in the Build A/B automated clean-room gate: the signed installed App now contains the MCP server, stable launcher and required runtime assets. A genuinely separate macOS login remains the final human check. | The installed product exposes a stable MCP command that survives package upgrades without requiring the source repository. | Automated PASS: Build A and replacement Build B both opened, attached, read and closed from a new HOME outside the repo with the unchanged `/Applications/Saccade.app/Contents/MacOS/saccade-current-tab-mcp` command. Evidence: `runs/dogfood/df_r14_installed_build_a3_20260717/report.json`, `runs/dogfood/df_r14_installed_build_b_20260717/report.json`. Mark final PASS after the separate-user login check. |
| DF-004 | P1 release-owner action, prepared | Distribution | Build 62 is Developer ID signed with Hardened Runtime, secure timestamp and scoped helper JIT entitlements. Its no-upload notarization preflight passes; the dogfood App/DMG are intentionally not submitted or stapled. | An install build intended for public distribution passes Gatekeeper through normal double-click installation. | Engineering preparation is complete. For the frozen public candidate: submit App and DMG, staple both tickets, and pass offline `spctl` on a second clean Mac. Local-only dogfood builds remain labeled clearly. |
| DF-005 | P0 | Multi-tab discovery | The browser can hold more than one Agent On browser ID, but the owner-only current pointer publishes only the focused tab and `saccade.tabs.list` returns only the current MCP process's in-memory tab state. With two visible Agent On tabs, an LLM discovers only one. | The browser publishes a safe registry of every Agent-eligible tab with opaque ID, title/origin, active state and ownership. It excludes page contents and capabilities. “Current tab” resolves only the visible tab; reading multiple tabs requires explicit task intent and separate revision-bound attachment. | With two Agent On tabs and one Off tab, list returns exactly the two eligible tabs, marks the visible one, exposes no protected data/capability, and allows deterministic attachment to either opaque tab ID. |
| DF-006 | P0 | Consent versus activity | `saccade.tabs.pause_agent` calls the CEF `pause` capability, which erases the current browser ID from `agent_granted_browser_ids_`; the visible human-controlled switch therefore changes from Agent On to Off. An Agent pause currently revokes human consent. | Split per-tab human permission (`Off`/`On`) from Agent runtime state (`disconnected`/`idle`/`working`/`paused`). Only a human UI action may change permission for a human-created tab. An Agent may pause/detach its work without flipping the switch. A tab created because the user asked an LLM to start a session is born Agent On, and the human can always turn it Off. | Agent pause/detach leaves a human-created tab On; Agent cannot turn an Off human tab On; LLM-started tab is automatically On; human Off immediately blocks further reads/actions; reconnect reflects activity separately from permission. |
| DF-008 | P0 product | Remove Saccade-owned site-action policy | The current contract and CEF adapter classify or block submit, payment, publication and other side effects. The user wants the LLM host to own gameplay, purchase, spending, messaging and submission policy. Saccade should carry less product-policy responsibility. | Saccade enforces Human-controlled per-tab Agent On/Off, model-invisible protected values, revision-bound input, target verification and receipts. It returns action facts but does not decide whether the LLM should play, buy, spend, message, publish or submit. | On an Agent On SimpleMMO tab, the LLM host can execute gameplay and purchase-flow actions without a Saccade-specific confirmation object. SSN/card/secret values remain absent from model context and evidence, stale actions still fail, and each executed input returns a receipt. |
| DF-009 | P1 | Browser navigation contract | `saccade.browser.navigate` advertises `back`, `forward` and `reload`, but the CEF engine capability list exposes only `navigate`; the live Back request failed and restoration required direct URL navigation. | MCP schemas and the selected engine's advertised capabilities agree, and common browser navigation operates through the current visible WebView. | Back, Forward and Reload pass same-WebView gates from source and installed package, or unsupported operations are omitted from the advertised tool contract. |
| DF-010 | P1 | Action-map canonicalization | SimpleMMO exposed duplicate logical controls as both button and link actions, including All, System, messages and notifications. | One logical target produces one stable canonical action with source aliases retained only as diagnostics. | The SimpleMMO event map contains one All, one System and one action per logical menu target without losing hit-test verification. |
| DF-011 | P1 | Collector readiness | The first AI-041 `/events` article request remained `renderer collector is not ready` through retries; the collector became healthy after same-origin navigation and stayed healthy through revisions 42–48. | A granted loaded tab self-initializes or returns a bounded retry state without requiring navigation away and back. | Attach immediately after tab selection and obtain truth/article/actions within the defined readiness deadline across 20 sequential dynamic-page runs. |
| DF-012 | P1 compatibility, engine-limited | IGN customized YouTube playback | Diagnosed in Build 61. Ordinary `youtube.com` video playback works through VP9/Opus-capable paths. IGN's recorded Chrome media route is HLS with `avc1` H.264 and `mp4a.40.2` AAC. The pinned CEF binary reports MSE VP9/Opus support but no H.264, AAC or native HLS support. | Unsupported codec/embed cases fail with a diagnosable compatibility reason; Saccade does not add a site-specific shim or distribute patent-encumbered codecs without a separate legal/build decision. | ROUTED: `runs/dogfood/df_build61_media_capabilities_20260718/report.json` identifies `pinned_cef_proprietary_codec_gap`; the prior Chrome request log is `runs/chrome_reference/ign_1781476351/chrome_network.json`. Keep ordinary YouTube as the green control. |
| DF-013 | P0 | Downloads | Fixed in installed build 44. Pinned CEF disabled downloads when the client returned no download handler. | Humans use Chromium's normal download UI. An Agent On tab may trigger a verified page download and receive a metadata-only completion receipt without local path, contents or execution authority. | PASS: source and installed fixture gates exposed no full path, contents or auto-execute authority; live Pixabay dogfood completed `alexzavesa-calm-elegant-logo-519008.mp3` through its two-step `Free download` flow with the same boundary. Evidence: `runs/dogfood/df_downloads_source_20260717/report.json`, `runs/dogfood/df_downloads_installed_20260717/report.json` and `runs/dogfood/df_pixabay_live_20260717/report.json`. |
| DF-014 | Fixed in signed Build 57 | Resize / stale layout | On signed Build 56, SimpleMMO was read at revision 2 and then narrowed across a responsive breakpoint. The old `View Updates` action was still accepted as `ok`, produced no verification receipt and did not navigate. | Browser-pushed layout epochs invalidate cached geometry. The MCP refreshes immediately before native input, locally rebases only the same surviving stable semantic action, requires a verified receipt, and rejects disappeared, covered or ambiguous targets before dispatch. | PASS: source and packaged native-window DOM/Canvas matrices rebased surviving actions without screenshots or another LLM turn, returned verified receipts, and rejected the resize-hidden target before input. A signed Build 57 live SimpleMMO rerun rebased `Show Chat` from revision 37/layout 36 to revision 38/layout 37 with a verified receipt, then rejected the vanished old `Register Account` action before input. Evidence: `runs/dogfood/df_build57_layout_epoch_source_20260718/report.json`, `runs/dogfood/df_build57_layout_epoch_packaged_20260718/report.json` and `runs/dogfood/df_build57_resize_live_simplemmo_20260718/report.json`; original failure: `runs/dogfood/df_build56_resize_stale_action_simplemmo_20260718/report.json`. |

## Required regression checks

| ID | Priority | Area | Check | Pass condition |
| --- | --- | --- | --- | --- |
| DF-R01 | P0 | Native browser basics | Launch the installed App directly. | Tabs, New Tab, address bar, Back, Forward, Reload/Stop and Command-L are visible and functional. |
| DF-R02 | P0 | Per-tab Agent consent | Create tabs through direct human action, link navigation and an LLM-started session. | Human-created tabs default to Agent Off; the LLM-created tab starts Agent On; navigation preserves only that tab's state; consent never leaks to another tab or child window. |
| DF-R03 | P0 | Running-session reuse | Keep Saccade running, then let the LLM start a Saccade session. | The existing process is reused and one foreground Agent On tab is created in the existing window. The LLM starts no second App process and takes over no Human tab. |
| DF-R04 | P0 | Current-tab truth | Turn Agent On for the visible tab and ask the LLM what page is open. | Zero-argument attach returns the visible tab's redacted URL/title/revision. Protected values, cookies, storage and capability tokens never appear. Baseline observed on 2026-07-16: `https://www.nanmesh.ai/agents`, title `Connect NaN Mesh to Your AI Agent`, revision 15. |
| DF-R05 | P0 | Protected fields | Fill an SSN/passport/driver-document field manually, then ask the LLM to fill ordinary remaining fields. | Protected original values travel only through the local secure path; the LLM, logs, screenshots and replay see state only; ordinary requested fields are filled; existing values are preserved; nothing is submitted. |
| DF-R06 | P0 | Multi-tab selection | Turn Agent On in two tabs, leave a third tab Off, and move focus between them. | The LLM can list safe metadata for exactly the two eligible tabs, “current” follows visible focus, no tab is read merely because it appears in the list, and the Off tab remains undiscoverable. |
| DF-R07 | P0 | Consent ownership | Exercise attach, read, idle, pause, disconnect and reconnect on a human-enabled tab, then start a new Agent-owned tab. | Runtime lifecycle never changes the human tab's On/Off permission; only the human switch does. The requested Agent-owned tab starts On and remains visibly revocable by the human. |
| DF-R08 | P0 | Switch truthfulness | Use three native tabs with On/Off/On permissions and cycle through them at least 50 times while sampling the UI, browser-selected ID, owner-only pointer and broker status. | No stale label or stale pointer occurs; the four observations agree at every sample, including immediately after close, reorder, navigation and foreground/background tab creation. Baseline pass on 2026-07-16: after the human turned both tested tabs Off, the broker returned `agent_enabled=false`, `paused=true`, and no URL/tab identity, so the LLM could not read either tab. |
| DF-R09 | P0 | Host-owned action policy | Let the LLM host apply its own gameplay policy across navigation, rewards, progression and a purchase flow. | Saccade adds no site-action confirmation prompt. It executes valid actions only while the tab is Agent On, protects model-invisible values, rejects stale input and records receipts. SimpleMMO baseline: System filter and All restore passed at revisions 47–48. |

## Withdrawn observations

- DF-007 was initially recorded as a possible switch/runtime mismatch. The user
  then clarified that both visible switches had been turned Off. The broker's
  Off result was therefore correct and the negative consent test passed. Keep
  DF-R08 as broader mixed-state regression coverage, not as evidence of a
  current defect.

## Fix order for the joint pass

1. DF-005/DF-006 tab registry and consent/activity state separation.
2. DF-008 remove Saccade-owned site-action policy while preserving data isolation and receipts.
3. DF-001 tab-versus-popup routing.
4. DF-002 repeat Keychain prompt reproduction and identity check.
5. DF-009/DF-010/DF-011 navigation, action-map and collector reliability.
6. DF-R01 through DF-R09 as one installed-build regression pass.
7. DF-003 self-contained MCP installation.
8. DF-004 notarized distribution after the product behavior is stable.

## Scope guard

- Preserve legitimate OAuth, passkey, payment and modal child windows.
- Do not infer popup completion from URL, cookies or provider-specific rules.
- Do not broaden Agent authority while fixing browser chrome or packaging.
- Do not expose protected identifiers, Keychain material or MCP capabilities in
  diagnostics.
