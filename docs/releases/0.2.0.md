# Saccade 0.2.0

Saccade 0.2.0 gives MCP-compatible Agents a live semantic view of one
authorized Chrome or Edge tab and exact object actions with local verification.
It ships with Extension 0.4.0.

## What is new

- Exact Agent sessions and tab leases, including one writer per tab and safe
  orphaning after a disconnect.
- Explicit full and delta Truth reads for one `tab_id`, backed by canonical
  current Truth in the Broker.
- One end-to-end deadline across Broker and Extension work.
- Local actionability waits and value-free semantic postconditions in action
  receipts.
- Preflighted independent form batches with compact per-step verification.
- Current support for contenteditable and same-origin iframe rich text, native
  and ARIA choices, standard uploads, semantic tables, and fast moving targets.
- Broker restart recovery without replaying an action whose outcome may be
  ambiguous.

## Release evidence

The exact Extension candidate passed the blocking gate in current Chrome and
Edge. Each browser completed controls, frames, tables, form batches, rich text,
legacy administration, standard upload, session isolation, replacement-stale
handling, and Mouse Accuracy.

Mouse Accuracy completed 96/96 exact-target actions across both browsers with
zero misses in this candidate run. See the
[machine-readable result](../evidence/release-0.2.0-mouse-accuracy.json) and the
[complete gate report](../reports/2026-08-30-saccade-0.2.0-release-gate.md).

Non-blocking compatibility checks also passed on Selenium's official web form,
DemoQA React, Angular Material, BestBuy, NaNMesh, NaNLogic, and Mythcast Era.
The public forms were not submitted.

## Artifacts

- npm package: `@nanlogic/saccade@0.2.0`
- npm dry-run size: 25,054 bytes
- npm shasum: `4945953270ac14d90e5f143b75d1231bd33f7b94`
- Extension: `saccade-extension-0.4.0.zip`
- Extension size: 71,261 bytes
- Extension SHA-256:
  `d61588adcd6a7100f9f36770ce20a634da07a62887adbe3b7fd81330040b2cbd`

## Install

```sh
npx -y @nanlogic/saccade install
npx -y @nanlogic/saccade doctor
```

Node.js 18 or newer is required. Install the matching Saccade Extension in
Chrome or Edge, then start a new Agent task.

## Scope

This release certifies the exact candidate and cases in the release report. It
does not claim compatibility with every authenticated site or every opaque
editor. Saccade reports unsupported or externally executed boundaries instead
of reporting an unverified action as complete.
