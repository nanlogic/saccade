--- a/tests/cefsimple/CMakeLists.txt
+++ b/tests/cefsimple/CMakeLists.txt
@@ -14,6 +14,8 @@ set(CEFSIMPLE_SRCS
   simple_handler.h
   saccade_adapter.cc
   saccade_adapter.h
+  saccade_brand_resources.cc
+  saccade_brand_resources.h
   )
 set(CEFSIMPLE_SRCS_LINUX
   cefsimple_linux.cc
--- a/tests/cefsimple/simple_app.h
+++ b/tests/cefsimple/simple_app.h
@@ -15,6 +15,9 @@ class SimpleApp : public CefApp, public CefBrowserProcessHandler {
   SimpleApp();
 
   // CefApp methods:
+  CefRefPtr<CefResourceBundleHandler> GetResourceBundleHandler() override {
+    return resource_bundle_handler_;
+  }
   CefRefPtr<CefBrowserProcessHandler> GetBrowserProcessHandler() override {
     return this;
   }
@@ -25,6 +28,8 @@ class SimpleApp : public CefApp, public CefBrowserProcessHandler {
   CefRefPtr<CefClient> GetDefaultClient() override;
 
  private:
+  CefRefPtr<CefResourceBundleHandler> resource_bundle_handler_;
+
   // Include the default reference counting implementation.
   IMPLEMENT_REFCOUNTING(SimpleApp);
 };
--- a/tests/cefsimple/simple_app.cc
+++ b/tests/cefsimple/simple_app.cc
@@ -11,6 +11,7 @@
 #include "include/views/cef_window.h"
 #include "include/wrapper/cef_helpers.h"
 #include "tests/cefsimple/simple_handler.h"
+#include "tests/cefsimple/saccade_brand_resources.h"
 
 namespace {
 
@@ -115,7 +116,8 @@ class SimpleBrowserViewDelegate : public CefBrowserViewDelegate {
 
 }  // namespace
 
-SimpleApp::SimpleApp() = default;
+SimpleApp::SimpleApp()
+    : resource_bundle_handler_(new SaccadeBrandResources()) {}
 
 void SimpleApp::OnContextInitialized() {
   CEF_REQUIRE_UI_THREAD();
